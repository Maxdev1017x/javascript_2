// E bem parecido com o FOR, mas ele permite que você percorra estrutura de dados com arrays, strings, maps...

const cars = ['Duster', 'Sandero', 'Clio'];

for (let car of cars) {
    console.log(car);
}
